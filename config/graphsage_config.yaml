model_params:
  channels: [1024, 512]

loader_params:
  num_neighbors: [10]
  batch_size: 256
  shuffle: true

input_data:
  graph_file_name: "BSARD_dataset/intermediate/hybrid_graph_full_wsem.pkl" 

optimizer_params:
  learning_rate: 0.0005

training_params:
  num_epochs: 20

bundle_tag: "BSARD_test"